# Blockchain Setup

The blockchain setup was done using a hardhat node to simulate real environments.
A faucet was also developed to make it as close to a mainnet EVM so that wallets could be created and directly used for challenges.

You can find the blockchain setup at the `setup` directory.